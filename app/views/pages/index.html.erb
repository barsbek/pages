<p id="notice"><%= notice %></p>

<%= link_to 'New Page', new_page_path %>

<% @pages.each do |subpages| %>
  <%= render 'subpages', pages: subpages %>
  <% unless subpages == @pages.last %>
    <hr>
  <% end %>
<% end %>
